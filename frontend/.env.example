# Frontend Environment Variables
VITE_API_BASE_URL=http://127.0.0.1:8000

# For the Capacitor Android build, Vite instead loads .env.production, which
# must point at a LAN-reachable or deployed backend host — "localhost" resolves
# to the device itself inside the app's WebView, not your dev machine.
